#! /usr/bin/env bash
sudo cp `which find` `which find`.bak
sudo cp `which gfind` `which find`

sudo cp `which dirname` `which dirname`.bak
sudo cp `which gdirname` `which dirname`

sudo cp `which readlink` `which readlink`.bak
sudo cp `which greadlink` `which readlink`
